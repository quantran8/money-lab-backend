# Source

Backend application (NestJS).

**Architecture:** See [docs/architecture.md](../docs/architecture.md) for layers, folder structure, and coding rules.

**Structure:** Feature-based modules (`auth`, `budget-simulation`, `learn`), shared `common/`, and infrastructure (`prisma`, `supabase`).
